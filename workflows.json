{
  "Start application": {
    "command": "npm run dev",
    "env": {
      "NODE_ENV": "development",
      "PORT": "5000",
      "SESSION_SECRET": "sas-secure-session-secret-key-2024",
      "DATABASE_URL": "${DATABASE_URL}",
      "PGHOST": "${PGHOST}",
      "PGPORT": "${PGPORT}",
      "PGUSER": "${PGUSER}",
      "PGPASSWORD": "${PGPASSWORD}",
      "PGDATABASE": "${PGDATABASE}"
    }
  }
}